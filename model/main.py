print("welcome to the project")


# from model.bilstm_model import create_bilstm_model
# import numpy as np

# # Load your preprocessed data (replace with actual data loading)
# X_train = np.load('path/to/preprocessed_data/X_train.npy')  # Adjust path to your data
# y_train = np.load('path/to/preprocessed_data/y_train.npy')

# # Define parameters
# vocab_size = 10000
# embedding_dim = 128
# lstm_units = 64
# max_sequence_length = 100

# # Create and compile the model
# model = create_bilstm_model(vocab_size, embedding_dim, lstm_units, max_sequence_length)
# model.compile(optimizer='adam', loss='binary_crossentropy', metrics=['accuracy'])

# # Train the model
# model.fit(X_train, y_train, epochs=10, batch_size=32, validation_split=0.2)
